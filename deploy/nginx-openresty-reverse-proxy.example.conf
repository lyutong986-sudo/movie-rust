# Movie Rust：整站反代到 Rust（API + 静态资源 + SPA 均由后端处理）
#
# 1. 在 http { } 内增加 WebSocket 升级映射（若尚未配置）：
#
#    map $http_upgrade $connection_upgrade {
#        default upgrade;
#        ''      close;
#    }
#
# 2. 将下面 server 合并进站点，或把 location / 拷入现有 server。
# 3. 删除原 `root .../index` 与 `try_files` / 静态 SPA 相关 location，避免与反代重复。
# 4. proxy_pass 改为 movie-rust 实际地址（本机 8096、或 docker 映射如 127.0.0.1:10004）。
#
#    nginx -t && openresty -s reload

# server {
#     listen 4443 ssl;
#     server_name test.emby.yun;
#
#     ssl_certificate     /www/sites/test.emby.yun/ssl/fullchain.pem;
#     ssl_certificate_key /www/sites/test.emby.yun/ssl/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     access_log /www/sites/test.emby.yun/log/access.log main;
#     error_log  /www/sites/test.emby.yun/log/error.log;
#
#     location / {
#         proxy_pass http://127.0.0.1:8096;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host  $host;
#         proxy_set_header X-Forwarded-Port  $server_port;
#
#         proxy_set_header Upgrade           $http_upgrade;
#         proxy_set_header Connection        $connection_upgrade;
#
#         proxy_connect_timeout 60s;
#         proxy_send_timeout    3600s;
#         proxy_read_timeout    3600s;
#
#         proxy_buffering off;
#     }
# }
